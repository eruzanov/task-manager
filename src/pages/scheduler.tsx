import { Calendar } from "components/calendar";

export const Scheduler = () => {
    return (
        <div style={{ padding: 24, background: 'white' }}>
            <Calendar />
        </div>
    );
};
